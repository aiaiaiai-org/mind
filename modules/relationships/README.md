# Relationships

Canonical authored relationships involving `organization:aiaiaiai`.

The machine-readable source is [`relationships.yaml`](relationships.yaml). This organization mind publishes its own relationship assertions independently of the personal `0x0sky/mind`; neither repository copies the other's authority.

## Current confirmation

The organization confirms the directed semantic relationship:

```text
person:0x0sky --member_of--> organization:aiaiaiai
```

The relationship is marked `reciprocal` because the canonical personal mind independently publishes the matching relationship as `member-of-aiaiaiai`.

`reciprocal` is based on two authored canonical assertions. GitHub organization membership may corroborate the relationship as provider-derived evidence, but provider discovery cannot create or upgrade canonical confirmation.

## Authority

Every relationship in this resource must involve `organization:aiaiaiai`, the manifest subject. Authored provenance authority is `organization:aiaiaiai`, the manifest publication owner.

The organization mind therefore speaks for its own endpoint only; it does not become authority for relationships between unrelated entities.
